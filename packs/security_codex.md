# 安全基线（Codex Online Pack - Example）

- 输入校验：后端对所有外部输入进行必填/类型/长度/格式校验。
- 注入防护：参数化查询；禁用字符串拼接 SQL/命令；输出转义与 CSP。
- 认证授权：默认拒绝；强制鉴权；最小权限；避免泄露具体错误原因。
- 秘密管理：密钥放入安全存储；不写入日志；区分环境（dev/prod）。
- 依赖安全：启用漏洞扫描；定期升级；锁定版本；阻止已知恶意包。
